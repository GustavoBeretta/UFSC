#carregando pacotes necessários
library(car)
#lendo a base de dados
base <- read.csv('euro2024_players.csv')
summary(base)
#montando modelo de regressão múltipla
modelo <- step(lm(log(MarketValue) ~ Age + log(Height) + Foot + log(Caps+1) + Goals, data = base))
#análises do modelo
summary(modelo)
qqPlot(modelo)
plot(fitted(modelo), rstandard(modelo))
abline(0,0)
#montando modelo de regressão múltipla
modelo <- step(lm(log(MarketValue) ~ Age + log(Height) + Foot + log(Caps+1) + Goals + log(Caps+1):Goals, data = base))
#análises do modelo
summary(modelo)
#montando modelo de regressão múltipla
modelo <- step(lm(log(MarketValue) ~ Age + log(Height) + Foot + log(Caps+1) + Goals + Caps:Goals, data = base))
#análises do modelo
summary(modelo)
#montando modelo de regressão múltipla
modelo <- step(lm(log(MarketValue) ~ Age + log(Height) + Foot + log(Caps+1) + Goals + log(Caps+1):Goals, data = base))
#análises do modelo
summary(modelo)
#montando modelo de regressão múltipla
modelo <- step(lm(log(MarketValue) ~ Age + log(Height) + Foot + log(Caps+1) + Goals + log(Caps+1):Goals + Age:log(Caps+1) + Age:Goals, data = base))
#análises do modelo
summary(modelo)
qqPlot(modelo)
plot(fitted(modelo), rstandard(modelo))
abline(0,0)
#montando modelo de regressão múltipla
modelo <- step(lm(log(MarketValue) ~ Age + log(Height) + Foot + log(Caps+1) + Goals + Age:log(Caps+1) + Age:Goals, data = base))
#análises do modelo
summary(modelo)
#montando modelo de regressão múltipla
modelo <- step(lm(log(MarketValue) ~ Age + log(Height) + Foot + log(Caps+1) + Goals + Age:log(Caps+1) + Age:Goals, data = base))
#análises do modelo
summary(modelo)
#montando modelo de regressão múltipla
modelo <- step(lm(log(MarketValue) ~ Age + log(Height) + Foot + log(Caps+1) + Goals + log(Caps+1):Goals + Age:log(Caps+1), data = base))
#análises do modelo
summary(modelo)
#montando modelo de regressão múltipla
modelo <- step(lm(log(MarketValue) ~ Age + log(Height) + Foot + log(Caps+1) + Goals + log(Caps+1):Goals + Age:log(Caps+1), data = base))
#análises do modelo
summary(modelo)
View(base)
#carregando pacotes necessários
library(car)
#lendo a base de dados
base <- read.csv('euro2024_players.csv')
summary(base)
#montando modelo de regressão múltipla
modelo <- step(lm(MarketValue ~ Age + Height + Foot + Caps + Goals, data = base))
#análises do modelo
summary(modelo)
qqPlot(modelo)
plot(fitted(modelo), rstandard(modelo))
abline(0,0)
#montando modelo de regressão múltipla
modelo <- step(lm(log(MarketValue) ~ log(Age) + log(Height) + log(Caps+1) + log(Goals+1), data = base))
#análises do modelo
summary(modelo)
qqPlot(modelo)
plot(fitted(modelo), rstandard(modelo))
abline(0,0)
#montando modelo de regressão múltipla
modelo <- step(lm(log(MarketValue) ~ log(Age) + log(Height) + log(Caps+1) + log(Goals+1) + log(Age):log(Caps+1) + log(Age):log(Goals+1) + log(Caps+1):log(Goals+1), data = base))
#análises do modelo
summary(modelo)
qqPlot(modelo)
plot(fitted(modelo), rstandard(modelo))
abline(0,0)
#montando modelo de regressão múltipla
modelo <- step(lm(log(MarketValue) ~ log(Age) + log(Height) + log(Caps+1) + log(Goals+1) + log(Age):log(Goals+1) + log(Caps+1):log(Goals+1), data = base))
#análises do modelo
summary(modelo)
#montando modelo de regressão múltipla
modelo <- step(lm(log(MarketValue) ~ log(Age) + log(Height) + log(Caps+1) + log(Goals+1) + log(Age):log(Caps+1) + log(Caps+1):log(Goals+1), data = base))
#análises do modelo
summary(modelo)
#montando modelo de regressão múltipla
modelo <- step(lm(log(MarketValue) ~ log(Age) + log(Height) + log(Caps+1) + log(Goals+1) + log(Age):log(Caps+1), data = base))
#análises do modelo
summary(modelo)
#montando modelo de regressão múltipla
modelo <- step(lm(MarketValue ~ log(Age) + log(Height) + log(Caps+1) + log(Goals+1) + log(Age):log(Caps+1) + log(Caps+1):log(Goals+1), data = base))
#análises do modelo
summary(modelo)
#montando modelo de regressão múltipla
modelo <- step(lm(log(MarketValue) ~ Age + log(Height) + log(Caps+1) + log(Goals+1) + log(Age):log(Caps+1) + log(Caps+1):log(Goals+1), data = base))
#análises do modelo
summary(modelo)
#montando modelo de regressão múltipla
modelo <- step(lm(log(MarketValue) ~ Age + log(Height) + log(Caps+1) + log(Goals+1) + Age:log(Caps+1) + log(Caps+1):log(Goals+1), data = base))
#análises do modelo
summary(modelo)
#montando modelo de regressão múltipla
modelo <- step(lm(log(MarketValue) ~ Age + log(Height) + log(Caps+1) + log(Goals+1) + log(Age):log(Caps+1) + log(Caps+1):log(Goals+1), data = base))
#análises do modelo
summary(modelo)
#montando modelo de regressão múltipla
modelo <- step(lm(log(MarketValue) ~ Age + Height + log(Caps+1) + log(Goals+1) + Age:log(Caps+1) + log(Caps+1):log(Goals+1), data = base))
#análises do modelo
summary(modelo)
#montando modelo de regressão múltipla
modelo <- step(lm(log(MarketValue) ~ Age + Caps + log(Goals+1) + Age:log(Caps+1) + log(Caps+1):log(Goals+1), data = base))
#análises do modelo
summary(modelo)
#montando modelo de regressão múltipla
modelo <- step(lm(log(MarketValue) ~ Age + log(Caps+1) + Goals + Age:log(Caps+1) + log(Caps+1):log(Goals+1), data = base))
#análises do modelo
summary(modelo)
#montando modelo de regressão múltipla
modelo <- step(lm(log(MarketValue) ~ Age + log(Caps+1) + log(Goals+1) + Age:log(Caps+1) + log(Caps+1):log(Goals+1), data = base))
#análises do modelo
summary(modelo)
qqPlot(modelo)
#montando modelo de regressão múltipla
modelo <- step(lm(log(MarketValue) ~ Age + log(Caps+1) + log(Goals+1) + Age:Caps + log(Caps+1):log(Goals+1), data = base))
#análises do modelo
summary(modelo)
#montando modelo de regressão múltipla
modelo <- step(lm(log(MarketValue) ~ Age + log(Caps+1) + log(Goals+1) + Age:log(Caps+1) + log(Caps+1):log(Goals+1), data = base))
#análises do modelo
summary(modelo)
qqPlot(modelo)
plot(fitted(modelo), rstandard(modelo))
abline(0,0)
#carregando pacotes necessários
library(car)
#lendo a base de dados
base <- read.csv('euro2024_players.csv')
summary(base)
#montando modelo de regressão múltipla
modelo <- step(lm(log(MarketValue) ~ Age + log(Caps+1) + log(Goals+1) + Age:log(Caps+1) + log(Caps+1):log(Goals+1), data = base))
#análises do modelo
summary(modelo)
qqPlot(modelo)
plot(fitted(modelo), rstandard(modelo))
abline(0,0)
#montando modelo de regressão múltipla
modelo <- step(lm(log(MarketValue) ~ Age + Foot + log(Caps+1) + log(Goals+1) + Age:log(Caps+1) + log(Caps+1):log(Goals+1), data = base))
#análises do modelo
summary(modelo)
