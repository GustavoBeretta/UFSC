#carregando pacotes necessários
library(car)
#lendo a base de dados
base <- read.csv('euro2024_players.csv')
summary(base)
#montando modelo de regressão múltipla
modelo <- step(lm(log(MarketValue) ~ Age + log(Caps+1) + log(Goals+1) + Age:log(Caps+1) + log(Caps+1):log(Goals+1), data = base))
#análises do modelo
summary(modelo)
qqPlot(modelo)
plot(fitted(modelo), rstandard(modelo))
abline(0,0)
#carregando pacotes necessários
library(car)
#lendo a base de dados
base <- read.csv('euro2024_players.csv')
summary(base)
setwd("C:/Users/gusta/Documents/GitHub/UFSC/Técnicas Estatísticas de Predição (R)/Trabalho")
#carregando pacotes necessários
library(car)
#lendo a base de dados
base <- read.csv('euro2024_players.csv')
summary(base)
#montando modelo de regressão múltipla
modelo <- step(lm(log(MarketValue) ~ Age + log(Caps+1) + log(Goals+1) + Age:log(Caps+1) + log(Caps+1):log(Goals+1), data = base))
#análises do modelo
summary(modelo)
qqPlot(modelo)
plot(fitted(modelo), rstandard(modelo))
abline(0,0)
View(base)
#montando modelo de regressão múltipla
modelo <- step(lm(log(MarketValue) ~ Position + Age + log(Caps+1) + log(Goals+1) + Age:log(Caps+1) + log(Caps+1):log(Goals+1), data = base))
#análises do modelo
summary(modelo)
qqPlot(modelo)
plot(fitted(modelo), rstandard(modelo))
abline(0,0)
#carregando pacotes necessários
library(car)
#lendo a base de dados
base <- read.csv('euro2024_players.csv')
summary(base)
#montando modelo de regressão múltipla
modelo <- step(lm(log(MarketValue) ~ Position + Age + log(Caps+1) + log(Goals+1) + Age:log(Caps+1) + log(Caps+1):log(Goals+1), data = base))
#análises do modelo
summary(modelo)
#montando modelo de regressão múltipla
modelo <- lm(log(MarketValue) ~ Position + Age + log(Caps+1) + log(Goals+1) + Age:log(Caps+1) + log(Caps+1):log(Goals+1), data = base)
#análises do modelo
summary(modelo)
qqPlot(modelo)
plot(fitted(modelo), rstandard(modelo))
abline(0,0)
#montando modelo de regressão múltipla
modelo <- step(lm(log(MarketValue) ~ Age + Club + log(Caps+1) + log(Goals+1) + Age:log(Caps+1) + log(Caps+1):log(Goals+1), data = base))
#análises do modelo
summary(modelo)
qqPlot(modelo)
plot(fitted(modelo), rstandard(modelo))
abline(0,0)
#carregando pacotes necessários
library(car)
#lendo a base de dados
base <- read.csv('euro2024_players.csv')
summary(base)
#montando modelo de regressão múltipla
modelo <- step(lm(log(MarketValue) ~ Age + Club + log(Caps+1) + log(Goals+1) + Age:log(Caps+1) + log(Caps+1):log(Goals+1), data = base))
#análises do modelo
summary(modelo)
qqPlot(modelo)
plot(fitted(modelo), rstandard(modelo))
abline(0,0)
View(base)
length(base$Club)
length(base$Age)
#montando modelo de regressão múltipla
modelo <- step(lm(log(MarketValue) ~ Age + Club + log(Caps+1) + log(Goals+1) + Age:log(Caps+1) + log(Caps+1):log(Goals+1), data = base))
#análises do modelo
summary(modelo)
#montando modelo de regressão múltipla
modelo <- step(lm(log(MarketValue) ~ Age + Club + log(Caps+1) + log(Goals+1) + Age:log(Caps+1) + log(Caps+1):log(Goals+1), data = base))
#análises do modelo
summary(modelo)
qqPlot(modelo)
#montando modelo de regressão múltipla
modelo <- step(lm(log(MarketValue) ~ Age + log(Caps+1) + log(Goals+1) + Age:log(Caps+1) + log(Caps+1):log(Goals+1), data = base))
#análises do modelo
summary(modelo)
qqPlot(modelo)
plot(fitted(modelo), rstandard(modelo))
#montando modelo de regressão múltipla
modelo <- step(lm(log(MarketValue) ~ Age + Club + log(Caps+1) + log(Goals+1) + Age:log(Caps+1) + log(Caps+1):log(Goals+1), data = base))
#análises do modelo
summary(modelo)
qqPlot(modelo)
plot(fitted(modelo), rstandard(modelo))
abline(0,0)
#montando modelo de regressão múltipla
modelo <- step(lm(log(MarketValue) ~ Age + Club + log(Caps+1) + log(Goals+1) + Country + Age:log(Caps+1) + log(Caps+1):log(Goals+1), data = base))
#análises do modelo
summary(modelo)
qqPlot(modelo)
plot(fitted(modelo), rstandard(modelo))
abline(0,0)
#montando modelo de regressão múltipla
modelo <- step(lm(log(MarketValue) ~ Age + log(Caps+1) + log(Goals+1) + Country + Age:log(Caps+1) + log(Caps+1):log(Goals+1), data = base))
#análises do modelo
summary(modelo)
qqPlot(modelo)
plot(fitted(modelo), rstandard(modelo))
abline(0,0)
#montando modelo de regressão múltipla
modelo <- step(lm(MarketValue ~ Age + Caps + Goals + Country, data = base))
#análises do modelo
summary(modelo)
qqPlot(modelo)
plot(fitted(modelo), rstandard(modelo))
abline(0,0)
#montando modelo de regressão múltipla
modelo <- step(lm(MarketValue ~ Age + Caps + Goals + Country + Age:log(Caps+1) + log(Caps+1):log(Goals+1), data = base))
#análises do modelo
summary(modelo)
qqPlot(modelo)
plot(fitted(modelo), rstandard(modelo))
#montando modelo de regressão múltipla
modelo <- step(lm(MarketValue ~ Age + Caps + log(Goals+1) + Country + Age:log(Caps+1) + log(Caps+1):log(Goals+1), data = base))
#análises do modelo
summary(modelo)
qqPlot(modelo)
plot(fitted(modelo), rstandard(modelo))
abline(0,0)
#montando modelo de regressão múltipla
modelo <- step(lm(MarketValue ~ Age + log(Caps+1) + log(Goals+1) + Country + Age:log(Caps+1) + log(Caps+1):log(Goals+1), data = base))
#análises do modelo
summary(modelo)
qqPlot(modelo)
plot(fitted(modelo), rstandard(modelo))
abline(0,0)
#montando modelo de regressão múltipla
modelo <- step(lm(log(MarketValue) ~ Age + log(Caps+1) + log(Goals+1) + Country + Age:log(Caps+1) + log(Caps+1):log(Goals+1), data = base))
#análises do modelo
summary(modelo)
qqPlot(modelo)
plot(fitted(modelo), rstandard(modelo))
abline(0,0)
#montando modelo de regressão múltipla
modelo <- step(lm(log(MarketValue) ~ Age + Caps + Goals + Country + Age:log(Caps+1) + log(Caps+1):log(Goals+1), data = base))
#análises do modelo
summary(modelo)
qqPlot(modelo)
plot(fitted(modelo), rstandard(modelo))
abline(0,0)
#montando modelo de regressão múltipla
modelo <- step(lm(log(MarketValue) ~ Age + Caps + Goals + Country + Age:log(Caps+1) + log(Caps+1):log(Goals+1), data = base))
#análises do modelo
summary(modelo)
qqPlot(modelo)
plot(fitted(modelo), rstandard(modelo))
abline(0,0)
#montando modelo de regressão múltipla
modelo <- step(lm(log(MarketValue) ~ Age + Caps + Goals + Country, data = base))
#análises do modelo
summary(modelo)
qqPlot(modelo)
plot(fitted(modelo), rstandard(modelo))
abline(0,0)
#montando modelo de regressão múltipla
modelo <- step(lm(log(MarketValue) ~ Age + Club + Caps + Goals + Country, data = base))
#análises do modelo
summary(modelo)
qqPlot(modelo)
plot(fitted(modelo), rstandard(modelo))
abline(0,0)
#carregando pacotes necessários
library(car)
#lendo a base de dados
base <- read.csv('euro2024_players.csv')
summary(base)
#montando modelo de regressão múltipla
modelo <- step(lm(log(MarketValue) ~ Age + Club + log(Caps+1) + log(Goals+1) + Country + Age:log(Caps+1) + log(Caps+1):log(Goals+1), data = base))
#análises do modelo
summary(modelo)
qqPlot(modelo)
plot(fitted(modelo), rstandard(modelo))
abline(0,0)
options(max.print = 1000)
#análises do modelo
summary(modelo)
options(max.print = 1000)
#carregando pacotes necessários
library(car)
#lendo a base de dados
base <- read.csv('euro2024_players.csv')
summary(base)
#montando modelo de regressão múltipla
modelo <- step(lm(log(MarketValue) ~ Age + Club + log(Caps+1) + log(Goals+1) + Country + Age:log(Caps+1) + log(Caps+1):log(Goals+1), data = base))
#análises do modelo
summary(modelo)
qqPlot(modelo)
plot(fitted(modelo), rstandard(modelo))
abline(0,0)
options(max.print = 1000)
#carregando pacotes necessários
library(car)
#lendo a base de dados
base <- read.csv('euro2024_players.csv')
summary(base)
#montando modelo de regressão múltipla
modelo <- step(lm(log(MarketValue) ~ Age + Club + log(Caps+1) + log(Goals+1) + Country + Age:log(Caps+1) + log(Caps+1):log(Goals+1), data = base))
#análises do modelo
summary(modelo)
#análises do modelo
print(summary(modelo), max=1000)
qqPlot(modelo)
#análises do modelo
print(summary(modelo), max=1000)
options(max.print = 1000)
#carregando pacotes necessários
library(car)
#lendo a base de dados
base <- read.csv('euro2024_players.csv')
summary(base)
#montando modelo de regressão múltipla
modelo <- step(lm(log(MarketValue) ~ Age + Club + log(Caps+1) + log(Goals+1) + Country + Age:log(Caps+1) + log(Caps+1):log(Goals+1), data = base))
#análises do modelo
print(summary(modelo), max=1000)
qqPlot(modelo)
plot(fitted(modelo), rstandard(modelo))
abline(0,0)
#análises do modelo
summary(base, max = 1000)
#análises do modelo
summary(modelo, max = 1000)
#análises do modelo
sink("summary_output.txt")
summary(modelo)
sink()
#análises do modelo
sink("summary_output.txt")
summary(modelo)
sink()
