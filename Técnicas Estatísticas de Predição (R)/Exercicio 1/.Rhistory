setwd("C:/Users/gusta/Downloads")
setwd("C:/Users/gusta/Downloads/Exercicio1")
base = read.csv2("apartamento.csv", dec=".")
View(base)
View(base)
base$Local = as.factor(base$Local)
View(base)
base$Local = as.factor(base$Local)
#criando amostra
amostra <- base[sample(nrow(base), 80), ]
View(amostra)
#criando modelo de regressão
modelo <- lm(Valor ~ Area + Idade + Energia + Local, data = base)
summary(modelo)
#leitura da base de dados
base = read.csv2("apartamento.csv", dec=".")
base$Local = as.factor(base$Local)
View(amostra)
#leitura da base de dados
base = read.csv2("apartamento.csv", dec=".")
base$Local = as.factor(base$Local)
#criando amostra
amostra <- base[sample(nrow(base), 80), ]
#criando modelo de regressão
modelo <- lm(Valor ~ Area + Idade + Energia + Local, data = base)
summary(modelo)
View(modelo)
View(base)
View(amostra)
#gráficos
qqPlot(modelo)
#gráficos
ggPlot(modelo)
#gráficos
lybrary (car)
#gráficos
library (car)
#gráficos
library (car)
qqPlot(modelo)
plot(fitted(modelo), rstandard(modelo))
abline(0,0)
