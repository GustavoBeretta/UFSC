Para rodar o código basta abrir o terminal e colocar os seguintes comandos.

gcc -g "<caminho da pasta>\main.cpp" -o "<caminho da pasta>\main.exe" -lpthread

Após isso só colocar o comando.

.\main.exe 
